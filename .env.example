# ============================================
# CloudFlare R2 存储配置
# ============================================
# 如何获取这些信息：
# 1. 登录 CloudFlare Dashboard
# 2. 进入 R2 Object Storage
# 3. 创建或选择一个 Bucket
# 4. 点击 "Manage R2 API Tokens" 创建 API Token
# 5. 账户 ID 在右侧边栏可以找到

# R2 Access Key ID (创建 API Token 后获得)
AWS_ACCESS_KEY_ID=your_r2_access_key_id

# R2 Secret Access Key (创建 API Token 后获得，只显示一次，请妥善保存)
AWS_SECRET_ACCESS_KEY=your_r2_secret_access_key

# R2 Endpoint (格式: https://<account_id>.r2.cloudflarestorage.com)
# 将 <account_id> 替换为你的 CloudFlare 账户 ID
AWS_ENDPOINT=https://your_account_id.r2.cloudflarestorage.com

# R2 Region (固定使用 "auto")
AWS_DEFAULT_REGION=auto

# ============================================
# 可选配置（通常不需要修改）
# ============================================

# 请求超时时间（默认30秒）
# TIMEOUT=60s

# 连接超时时间（默认5秒）
# CONNECT_TIMEOUT=10s

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件为 .env
# 2. 填写你的 R2 凭证信息
# 3. 修改 config.toml 中的 database.uri 为: s3://your-bucket-name/papersys
# 4. LanceDB 会自动读取这些环境变量连接到 R2

